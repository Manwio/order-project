package order;


public class OrderTest {
    /*
    private Pizza pizza;

    double percentVal;

    @BeforeEach
    void setUp() {
        CheckBox b1, b2, c1, c2, c3, c4, t1, t2, t3, t4, t5, t6, t7, t8;

        b1.setText("Paradicsom");

        b1.setSelected(true);
        c3.setSelected(true);
        t2.setSelected(true);
        t4.setSelected(true);
        t6.setSelected(true);

        c2.setSelected(false);
        t1.setSelected(false);
        pizza = new Pizza(b1, b2, c1, c2, c3, c4, t1, t2, t3, t4, t5, t6, t7, t8);

    }

    @Test
    void testWhatsOnPizza() {
        String pizzaI = (b1.getText() + pizza.cheese3.getText() + pizza.topping2.getText() + pizza.topping4.getText() + pizza.topping6.getText());

        String pizzaItems = pizza.whatsOnPizza();
        assertEquals(pizzaItems, pizzaI);
        assertNotEquals(pizzaItems, pizzaI);
    }
    */
    /*
    @Test
    void testPayableCalculation() {
        double pval = pizza.payableCalculation();
        assertEquals(pval, (1500.0 * (1- pizza.percentVal * 0.01)));
    }
    */
}
